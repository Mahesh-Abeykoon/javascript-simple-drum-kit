var drumButtonImages = document.querySelectorAll(".drum").length;

for(var i = 0; i < drumButtonImages; i++){

    document.querySelectorAll(".drum")[i].addEventListener("click", something);
        function something (){
            // alert('clicked')
    }
}
